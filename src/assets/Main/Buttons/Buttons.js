import Button from "./Button/Button"

const Buttons = () => {
    return (
        <div>
            <Button />
            <Button />
            <div>A fee may apply.</div>
        </div>
    )
}

export default Buttons;